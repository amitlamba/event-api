spring:
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://192.168.0.109:8888
      enabled: true
      name: event-api
  application:
    name: event-api
  mvc:
    view:
      prefix:

